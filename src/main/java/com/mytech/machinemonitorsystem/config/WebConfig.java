/*
package com.mytech.machinemonitorsystem.config;

import org.springframework.boot.web.servlet.FilterRegistrationBean;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.boot.web.servlet.support.ErrorPageFilter; // Ensure this import is correct

@Configuration
public class WebConfig { // Or your existing Configuration class
    @Bean
    public FilterRegistrationBean<ErrorPageFilter> errorPageFilter() {
        FilterRegistrationBean<org.springframework.boot.web.servlet.support.ErrorPageFilter> registrationBean = new FilterRegistrationBean<>();
        registrationBean.setFilter(new org.springframework.boot.web.servlet.support.ErrorPageFilter());
        registrationBean.setEnabled(false);
        return registrationBean;
    }
}

*/
